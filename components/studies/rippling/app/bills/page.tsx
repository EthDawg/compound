import { PageHeader, Empty } from "@/components/ui";

export default function Page() {
  return (
    <div>
      <PageHeader eyebrow="Finance · Bill pay" title="Bill pay" sub="Vendor approvals route through the same computed chain as everything else. Bill pay is one of the few surfaces where the core object is not the employee — which made it the hardest of the four-question tests to pass." />
      <Empty
        title="Not built out in this study"
        body="This product line exists in the navigation because the point of the navigation is that it is long. The surfaces that carry the argument — People, Hiring, Payroll, Devices, Apps, Spend, Workflows and the Graph explorer — are built."
        note="Vendor approvals route through the same computed chain as everything else. Bill pay is one of the few surfaces where the core object is not the employee — which made it the hardest of the four-question tests to pass."
      />
    </div>
  );
}
